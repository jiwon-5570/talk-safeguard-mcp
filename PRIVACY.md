# 개인정보 처리 원칙

## 최소 처리와 비저장

- 카카오톡 메시지 원문은 요청 처리 중 메모리에서만 분석하며 파일, 데이터베이스, 캐시, 분석 로그에 저장하지 않습니다.
- 전화번호, 계좌번호 후보, 사업자등록번호, 인증번호와 개인정보 요구 표현은 현재 응답을 만들기 위해서만 사용합니다.
- `/debug/analyze`도 원문을 로그에 남기지 않으며 HTTP 응답에는 `Cache-Control: no-store`를 적용합니다.
- 데이터 보관 기간은 없습니다. 서버가 별도의 사용자 데이터 저장소를 만들지 않습니다.

## 로그와 요청 제한

구조 로그에는 이벤트명, 포트, 오류 종류처럼 비식별 운영 정보만 기록합니다. 메시지·URL·전화번호·계좌번호를 metadata에 전달하지 않습니다.

Rate limit은 요청 횟수 계산을 위해 원본 IP를 프로세스별 임의 salt와 함께 SHA-256 해시한 키로 바꾸어 메모리에 잠시 보관합니다. 원본 IP와 메시지는 저장하지 않으며 제한 시간이 지나면 항목을 제거하고 서버 재시작 시 전체 상태가 사라집니다.

## 외부 API 전송

- `PUBLIC_DATA_MODE=sample`에서는 외부 공공데이터 API로 사용자 입력을 전송하지 않습니다.
- `PUBLIC_DATA_MODE=actual`이고 `NTS_BUSINESS_API_KEY`가 설정되면 국세청 API에 정규화된 사업자등록번호 10자리만 전송합니다.
- `PUBLIC_DATA_MODE=actual`이고 `FTC_ONLINE_SELLER_API_KEY`가 설정되면 공정위 API에 사업자등록번호 또는 상호명을 전송합니다.
- 메시지 전체, 전화번호, 계좌번호, 인증번호는 사업자 조회 API로 전송하지 않습니다.
- API 실패 시 sample fallback으로 전환하며 입력을 별도로 보관하지 않습니다.

운영자는 reverse proxy, APM, 클라우드 액세스 로그가 요청 본문이나 query parameter를 수집하지 않도록 별도로 설정해야 합니다.
