# 보안 정책

## 안전 판정 원칙

톡세이프가드는 개인·전화번호·계좌·사업자를 사기 또는 범죄 수단으로 확정 판정하지 않습니다. 결과는 관찰된 위험 신호와 권장 확인 절차이며 오탐과 미탐이 발생할 수 있습니다. LOW 결과도 안전을 보장하지 않습니다.

이미 송금, 앱 설치, 개인정보 제공이 발생했다면 추가 행동을 중단하고 해당 은행 고객센터, 경찰 112, 금융감독원 1332 등 공식 경로에 즉시 문의하세요. 스미싱 상담은 한국인터넷진흥원 118 등 공식 채널을 이용하세요.

## 취약점 제보

GitHub 저장소의 Security 탭에서 private vulnerability reporting 또는 Security Advisory를 이용해 재현 절차, 영향 범위, 영향을 받는 버전을 알려주세요. 비공개 제보 기능을 사용할 수 없다면 공개 이슈에는 공격 코드·토큰·개인정보를 올리지 말고, 안전한 제보 채널을 요청하는 최소 정보만 남겨주세요.

실제 API 키, 사용자 메시지, 전화번호, 계좌번호, 인증번호는 제보 자료에서 제거해야 합니다.

## 운영 배포 기준

- 외부 트래픽에는 HTTPS를 적용합니다.
- `ALLOWED_ORIGINS`를 실제 클라이언트 Origin으로 제한합니다.
- API 키는 secret manager나 배포 환경변수로 주입하고 저장소에 커밋하지 않습니다.
- `ENABLE_DEBUG_ENDPOINT=false`를 권장하고 필요 시 별도 접근 제어를 적용합니다.
- `RATE_LIMIT_WINDOW_MS`, `RATE_LIMIT_MAX`를 트래픽 특성에 맞게 설정합니다.
- 신뢰할 수 있는 reverse proxy 뒤에서만 `TRUST_PROXY=true`를 사용합니다.
- 로그에는 메시지·URL·전화번호·계좌번호·인증정보 원문을 남기지 않습니다.
- 의존성 취약점, Node.js 보안 업데이트와 공공데이터 API 변경을 주기적으로 확인합니다.

`helmet`, CORS allowlist, 요청 본문 64KB 제한, `Cache-Control: no-store`, IP 해시 기반 메모리 rate limit은 애플리케이션 기본 방어선이며 배포 플랫폼의 방화벽·인증·모니터링을 대체하지 않습니다.
